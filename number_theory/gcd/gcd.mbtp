// The result is non-negative and divides both inputs
predicate gcd_ok(a : Int, b : Int, result : Int) {
  (0 <= result) &&
  (result <= a || a < 0) &&
  (result <= b || b < 0)
}

// Helper: result is non-negative
predicate gcd_nonneg(result : Int) {
  0 <= result
}
