import {
  "moon-certified/utils",
}

// Suppress false-positive unused_package warning: @utils.SplitMix64 is used
// in generator function signatures and SplitMix64::new constructor calls,
// but the compiler's unused_package analysis misses type-only references.

warnings = "-unused_package"

options(
  "proof-enabled": false,
)
